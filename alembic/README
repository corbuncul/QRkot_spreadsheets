# Миграции

В проекте уже имееются необходимые для работы миграции. В случае с новой базой данных их неоходимо только применить.
В случае внесения изменений в модели SQLAlchemy необходимо сгенерировать новую миграцию и затем применить.
При необходимости миграции можно откатить. 

## Создание новой миграции:

```bash
alembic revision --autogenerate -m "Название миграции"
```

## Примененние миграции:

```bash
alembic upgrade head
```

## Откат последней миграции:

```bash
alembic downgrade -1
```

## Последняя применённая миграция:

```bash
alembic current
```

## Просмотр истории миграций:

```bash
alembic history
```

или с выводом текущей версии миграции:

```bash
alembic history -i
```
или подробный вывод:

```bash
alembic history -v
```